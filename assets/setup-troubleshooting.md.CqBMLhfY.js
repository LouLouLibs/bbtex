import{_ as t,o,c as i,a2 as a}from"./chunks/framework.Cv-B11KK.js";const u=JSON.parse('{"title":"Setup and troubleshooting","description":"","frontmatter":{},"headers":[],"relativePath":"setup-troubleshooting.md","filePath":"setup-troubleshooting.md","lastUpdated":1790314763000}'),n={name:"setup-troubleshooting.md"};function s(r,e,c,l,d,p){return o(),i("div",null,[...e[0]||(e[0]=[a(`<h1 id="setup-and-troubleshooting" tabindex="-1">Setup and troubleshooting <a class="header-anchor" href="#setup-and-troubleshooting" aria-label="Permalink to &quot;Setup and troubleshooting&quot;">​</a></h1><h2 id="common-problems" tabindex="-1">Common problems <a class="header-anchor" href="#common-problems" aria-label="Permalink to &quot;Common problems&quot;">​</a></h2><p><strong>A build fails with no useful message.</strong> Read the menu command&#39;s log with <code>cat ~/.local/state/bbtex/last-compile.log</code>, or run the build in Terminal with <code>bbtex compile --verbose main.tex</code> for the full output.</p><p><strong>&quot;No document open&quot;.</strong> The commands need BBEdit&#39;s <code>BB_DOC_PATH</code>. Run them from a saved document window, not a shell worksheet or an untitled buffer.</p><p><strong>Forward search does nothing.</strong> Skim must be in <code>/Applications</code> (or <code>~/Applications</code>), and the build must produce SyncTeX data, which latexmk does by default.</p><p><strong><code>latexmk</code> or <code>pdflatex</code> not found.</strong> BBEdit doesn&#39;t see your TeX binaries. MacTeX adds <code>/Library/TeX/texbin</code> to the system path. For TinyTeX or a custom location, add its <code>bin</code> directory to <code>/etc/paths.d/</code> or your shell profile.</p><p><strong>Anything else.</strong> Run <strong>Scripts → LaTeX — Doctor</strong> and read the report as described below.</p><h2 id="doctor" tabindex="-1">Doctor <a class="header-anchor" href="#doctor" aria-label="Permalink to &quot;Doctor&quot;">​</a></h2><p>For the supported MacTeX setup, use <strong>Biber ≥ 2.21</strong>, paired with a compatible biblatex version. The verified installation is MacTeX 2026 with Biber 2.21. Check with <code>biber --version</code>; the version alone does not prove the launcher works or that it matches a Tectonic bundle. Tectonic&#39;s older bundle requires separate Biber/biblatex compatibility validation.</p><p>Run <code>bbtex doctor</code> for inspection only, or <code>bbtex doctor --probe</code> for tool launch and version checks. <strong>Scripts → LaTeX — Doctor</strong> includes the launch checks. Supply a saved source with <code>bbtex doctor path/to/main.tex</code> to resolve its engine and inspect its latest bbtex build log. The menu uses <code>BB_DOC_PATH</code> when BBEdit supplies it; without that value it checks the default setup. The report appears as shell output.</p><p>For a relocated or staged BBEdit support folder, pass <code>bbtex doctor --bbedit-support &quot;/path/to/BBEdit Support&quot;</code>. Doctor scans Scripts and Packages below that directory, including renamed packages and nested menu folders. It follows directory links without revisiting the same location and reports incomplete inspection at 2,048 entries or eight levels. It does not search the whole disk. Renamed individual commands and external unselected support folders still require manual inspection.</p><p>Without <code>--probe</code>, inspection is strictly read-only: no tool execution, compilation, settings changes, directory creation or installation. With <code>--probe</code>, Doctor directly executes the first discovered tool with its version flag, without a shell, with disconnected stdin, a three-second limit per tool, and a 16 KiB combined stdout/stderr limit. Timeout/output-limit cleanup kills the probe process group. Probes run sequentially (up to seven tools, roughly 21 seconds of timeouts). External tools may initialize caches; no document is compiled and Doctor does not install or repair anything. RaTeX is never probed. <code>OK</code> means the stated inspection passed, not that the feature has been exercised. <code>WARN</code> needs attention; <code>OPTIONAL</code> is not a broken installation. <code>UNVERIFIED</code> requires a manual check. Exit status is zero when a report is produced, including reports containing warnings.</p><h2 id="interpreting-the-report" tabindex="-1">Interpreting the report <a class="header-anchor" href="#interpreting-the-report" aria-label="Permalink to &quot;Interpreting the report&quot;">​</a></h2><ul><li>Missing selected TeX engine or latexmk: install/repair your TeX distribution and make its executable directory available on PATH. The BBEdit launcher includes <code>/Library/TeX/texbin</code>, <code>/opt/homebrew/bin</code> and <code>/usr/local/bin</code> before inherited PATH. The terminal command inspects the terminal&#39;s PATH, which may differ.</li><li>Missing <code>pdftoppm</code>: install Poppler if you want equation previews.</li><li>Missing <code>texlab</code>: check the TexLab installation and BBEdit language-server configuration; executable discovery alone does not verify that BBEdit uses it.</li><li>Missing BibTeX/Biber: install the backend required by your bibliography. A Tectonic BibTeX project uses its embedded backend; external BibTeX is optional.</li><li>Biber universal launcher failure: the known local error is <code>extracting arm64 binary with lipo failed</code>. Repair/reinstall through your TeX distribution. This occurs before bibliography processing; changing citation keys will not fix it.</li><li>Biber control-file version mismatch: use a compatible Biber/biblatex pair. Tectonic&#39;s selected bundle supplies biblatex independently of the system TeX installation. The verified older bundle biblatex 3.17 requires isolated Biber 2.17; the supported MacTeX Biber 2.21 must not be substituted for that fixture. Doctor recognizes this if evidence remains in the latest build log. A successful Biber version query does not prove compatibility with a project&#39;s biblatex.</li><li>Duplicate commands: inspect both BBEdit&#39;s Scripts folder and the <code>bbtex.bbpackage</code> package. Keep one active workflow installation. <code>bbtex-support.bbpackage</code> is a separate editing package and is expected alongside the workflow commands.</li><li>Save attachment: new installations include a <code>.bbtex-receipt</code> checksum beside the compiled hook. <code>OK</code> means its contents match that receipt, not a security attestation. Modified/unreadable hooks are warnings; older hooks without receipts remain unverified. <code>Document</code> and <code>BBEdit</code> attachments are reported as possible conflicts. The installer refuses changed/conflicting hooks; a receipt-less legacy hook is adopted only if its full decompiled source matches the current hook. Follow the preview installation instructions; never delete unrelated attachments.</li><li>State access: the nearest existing parent must be writable/searchable. The inspection does not prove that an actual write will succeed (quota/ACL changes and other runtime failures remain possible).</li></ul><p>RaTeX remains an experimental placeholder. No installation is recommended by doctor. Tool versions and launchability require <code>--probe</code>. Bibliography compatibility, renamed individual commands and native integration still require their own checks. Version output is bounded and reduced to its first nonempty line; failure signatures are examined separately.</p><h2 id="disposable-smoke-check-and-sharing" tabindex="-1">Disposable smoke check and sharing <a class="header-anchor" href="#disposable-smoke-check-and-sharing" aria-label="Permalink to &quot;Disposable smoke check and sharing&quot;">​</a></h2><p>Create a new temporary folder and save <code>main.tex</code> containing:</p><div class="language-tex vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tex</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\documentclass</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{article}</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\begin</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{document}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Hello.</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\[</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> E = mc^2 </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\]</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\end</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{document}</span></span></code></pre></div><p>Run Doctor, compile from BBEdit, then select <code>E = mc^2</code> and preview it. Confirm that the full PDF opens in Skim, the equation preview appears, and editor focus returns as documented. Use a disposable document for structural insertion/Undo checks. Do not use an existing paper as a test fixture.</p><p>The diagnostic report replaces the current home directory with <code>~</code>. Other paths may remain, including project paths outside your home; review before sharing. It does not include raw logs or document contents. See <a href="./dev/releases">releases</a> for installation/upgrade instructions and <a href="./selection-preview">selection preview</a> for optional service/save-hook setup. Preserve a backup before changing an existing package; restore that backup to roll back an upgrade.</p><h2 id="fresh-account-or-fresh-mac-acceptance" tabindex="-1">Fresh-account or fresh-Mac acceptance <a class="header-anchor" href="#fresh-account-or-fresh-mac-acceptance" aria-label="Permalink to &quot;Fresh-account or fresh-Mac acceptance&quot;">​</a></h2><p>This remains a manual acceptance gate. Staged tests on an already configured Mac do not establish that the dependency/setup instructions suffice on a fresh machine. Use a disposable macOS account or a spare Mac; do not remove a working installation to simulate one. Record macOS/BBEdit/TeX versions and the package checksum.</p><ol><li>Install the documented prerequisites and the Apple Silicon release package from <a href="https://github.com/LouLouLibs/bbtex#prerequisites" target="_blank" rel="noreferrer">README</a>. Run <strong>LaTeX — Doctor</strong>, keeping the report and noting missing optional tools separately from required ones. Confirm there is only one active workflow installation and no unexplained attachment conflicts.</li><li>In a temporary folder, create the document above. Compile from BBEdit, open the PDF in Skim, and check forward/inverse search. Select <code>E = mc^2</code> and invoke <strong>Preview Selection</strong>; confirm a cropped equation and preserved editing focus.</li><li>Open <strong>Project Outline Window</strong>, navigate and save a heading change. Try a structural insertion followed by Undo. Enable optional preview-on-save only if desired; verify the installer leaves any unrelated attachment alone.</li><li>Back up the installed package and any bbtex-owned save hook <strong>with its receipt</strong>. Upgrade the package once and verify the same smoke checks. Restore that backup to test rollback; do not overwrite an attachment modified since the backup.</li><li>Close bbtex preview windows and disable preview-on-save. Quit BBEdit normally, then remove only the package installed for this test. Remove the optional <code>Document.documentDidSave.scpt</code> and its receipt only if still identified as the unchanged bbtex hook; retain shared or modified attachments for manual review. Remove the optional <code>LaTeX — Preview Selection.workflow</code> from Services only if installed in this walkthrough. Restart BBEdit and verify the removed commands are absent and unrelated tools still work. Project sources/PDFs and TeX itself are never uninstall targets. Retaining bbtex&#39;s cache/state is harmless.</li></ol><p>For automated staging, <code>bash test/integration/check_installation.sh</code> copies the built package into a temporary custom support folder, verifies discovery, upgrade refusal and removal, and preserves a foreign attachment. It never repurposes HOME or writes into the real BBEdit support folder. The hook installer&#39;s <code>--bbedit-support DIRECTORY</code> option selects its destination for this staging test; it does not configure BBEdit or change the hook&#39;s standard worker lookup paths.</p>`,24)])])}const b=t(n,[["render",s]]);export{u as __pageData,b as default};
