import{_ as i,o as s,c as t,a2 as n}from"./chunks/framework.Cv-B11KK.js";const u=JSON.parse('{"title":"Persistent outline: navigation prototype","description":"","frontmatter":{},"headers":[],"relativePath":"dev/outline-window.md","filePath":"dev/outline-window.md","lastUpdated":1790314763000}'),a={name:"dev/outline-window.md"};function o(r,e,d,p,l,h){return s(),t("div",null,[...e[0]||(e[0]=[n(`<h1 id="persistent-outline-navigation-prototype" tabindex="-1">Persistent outline: navigation prototype <a class="header-anchor" href="#persistent-outline-navigation-prototype" aria-label="Permalink to &quot;Persistent outline: navigation prototype&quot;">​</a></h1><p>This records the implementation and capability checks for issue #1. The persistent window is available as <strong>LaTeX — Project Outline Window</strong>, or <code>bbtex outline-window FILE</code>. One window stays pinned to each canonical project; other projects open separate windows. The launcher returns immediately once its preview exists and reuses the same session when invoked again. The existing <strong>LaTeX — Project Outline</strong> menu remains unchanged. The prototype uses the saved project index and opens a BBEdit HTML preview containing indented entry buttons in an expandable hierarchy. Disclosure triangles collapse/expand sections; heading buttons navigate. Search matches literal title, kind, context and file text, ignoring case and common combining accents. Matching descendants keep their ancestors visible. Clearing search (or Escape) restores expansion; Return in search opens the first match. Tab and Return use standard HTML buttons. Navigation runs <code>Outline.jump</code>, including disk fingerprints, dirty buffers and source-line checks. The navigation result is displayed inside the window.</p><p><strong>Refresh saved outline</strong> rereads the pinned project&#39;s saved sources without saving buffers or opening source windows. It preserves search, expansion, selected entry and scroll position. Entry identity uses file, kind, title, context and an occurrence number; moving an entry&#39;s lines preserves identity, while renaming or reordering identical duplicates may reset its state. Search temporarily expands matching branches and retains the underlying expansion state across refresh. Every refreshed snapshot has a revision: a request from an older snapshot is rejected rather than interpreted as a different entry. Refresh does not overwrite the preview HTML file, avoiding a BBEdit reload that would discard UI state.</p><p>The page also polls saved sources every two seconds using the same update path. Unchanged indexes return no content; changed snapshots are applied without source navigation or window activation. Requests never overlap navigation/refresh. Each poll includes the displayed revision, so a missed response is retried. This first implementation checks metadata first, rebuilding on changes, every 30 seconds, or while index warnings exist (including missing-input recovery). A 121-file, 9,600-entry benchmark took 39 ms to index and 33 ms to render; idle metadata checks averaged 0.116 ms on the development Mac. Native BBEdit startup/readback for 9,600 entries measured 1.23 s, with an automatic update applied in 1.53 s including the polling interval. Headless Chrome search through the next paint measured about 255 ms after batching filter mutations (previously 3.7 seconds). These are measurements on the development Mac, not release performance guarantees.</p><p>To try it from a development checkout:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">opam</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> exec</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dune</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">OUTLINE_SESSION</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mktemp</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /tmp/bbtex-outline.XXXXXX</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">_build/default/bin/main.exe</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> outline-window-prototype</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /absolute/path/main.tex</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$OUTLINE_SESSION</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span></span></code></pre></div><p>The command stays running while the window is open. Repeating it with the same source and session directory reuses that window. A different project requires another directory. Close the preview window or interrupt the owning command to stop the session. The temporary directory retains only its lock file afterward and can be removed once the process has stopped. An interrupted session may leave an inert preview window open; close that window manually.</p><h2 id="presentation-and-navigation-findings" tabindex="-1">Presentation and navigation findings <a class="header-anchor" href="#presentation-and-navigation-findings" aria-label="Permalink to &quot;Presentation and navigation findings&quot;">​</a></h2><p>BBEdit&#39;s <code>x-bbedit://open</code> URLs support source locations, but do not run our validation before opening them. The installed scripting dictionary exposes HTML preview windows without a JavaScript-to-AppleScript callback. This proof instead uses an OCaml loopback-only listener on an ephemeral port. JavaScript POSTs a snapshot entry number with a random 192-bit session token; requests cannot provide file paths, shell commands or AppleScript. The listener checks Host and Origin, rejects GET navigation and out-of-range entries, and bounds header reads and native jumps. Generated labels/context are HTML-escaped. This local endpoint is private to the prototype, not a public server or general-purpose command interface.</p><p>Source-controlled titles, context, paths and warnings are escaped for HTML text and double-quoted attributes; they are never interpolated into JavaScript. Refresh uses the same renderer. Status/error strings use <code>textContent</code>. Regression tests cover script closing tags, injected image/SVG event handlers, attribute breakouts and encoded entities in each source-controlled field. A Content Security Policy additionally permits only the generated script&#39;s random nonce, restricts network requests to this session endpoint, and blocks other resources, forms and base URLs. These checks address outline HTML injection; they are not a sandbox for compiling untrusted TeX documents.</p><p>The first form/iframe implementation failed the user&#39;s interaction check: BBEdit sent navigation to an external browser, which received “Unknown request”. Buttons now have no navigation target and use <code>fetch</code> instead; response text goes into an inline live status region. The next test exposed a second failure: BBEdit sends <code>Origin: x-bbedit-preview://</code>, so allowing only <code>null</code> rejected its fetch requests. The listener now accepts that exact origin as well as <code>null</code>, and returns the validated origin in its CORS response. The token, Host and POST checks remain enforced. A POST/readback handshake verifies the actual preview can read the response before displaying “Connected”. Actual click/Return acceptance remains confirmed for mouse navigation by the user after the origin fix. The subsequent tree/search UI was also accepted by the user. Refresh/state retention needs its own interaction check.</p><p>The page&#39;s heartbeat proves the HTML-to-loopback connection. It is not sufficient for lifecycle management: BBEdit can retain a closed page&#39;s executing JavaScript. The prototype also checks BBEdit&#39;s preview-window list every two seconds, through bounded AppleScript calls. Missing windows stop their worker. This polling tradeoff needs review before making the window a normal installed command.</p><h2 id="verification-and-remaining-scope" tabindex="-1">Verification and remaining scope <a class="header-anchor" href="#verification-and-remaining-scope" aria-label="Permalink to &quot;Verification and remaining scope&quot;">​</a></h2><p><code>test/test_outline_window.ml</code> covers escaping, token/entry/method authorization, and cross-origin/Host rejection. The explicit native integration test is:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">uv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test/integration/check_outline_window.py</span></span></code></pre></div><p>It uses disposable sources, verifies window reuse, drives the local endpoint into real native jumps, verifies the BBEdit POST/readback handshake, checks dirty/stale rejection, and checks closing the window stops the worker and removes its page. Browser click/Return behavior needs a real interaction check; HTTP-driven navigation alone does not establish keyboard UX. Native tests are intentionally not part of unattended CI.</p><p>The user accepted automatic updates. Remaining optional polish includes persistence across closing/reopening. Automatic refresh preserves focused entry/summary controls when the outline already has focus; native tests verify it leaves another editing window in front. The 16-request startup burst regression covers listener queueing. The window is now included in menu installation and release packaging. No unsaved indexing, source saving, or RaTeX changes were introduced. This prototype stays pinned to its initial project.</p><p>Explicit browser test (Playwright may be installed in a temporary directory):</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">PLAYWRIGHT_MODULE</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/path/to/playwright/index.mjs</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> \\</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">OUTLINE_BROWSER=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;/Applications/Google Chrome.app/Contents/MacOS/Google Chrome&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">node </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">test/integration/check_outline_browser.mjs</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">uv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test/integration/check_outline_large.py</span></span></code></pre></div>`,19)])])}const g=i(a,[["render",o]]);export{u as __pageData,g as default};
