import{_ as t,o as i,c as a,a2 as n}from"./chunks/framework.Cv-B11KK.js";const o="/bbtex/assets/outline-window.BrYm0sKj.png",m=JSON.parse('{"title":"Project outline","description":"","frontmatter":{},"headers":[],"relativePath":"project-navigation.md","filePath":"project-navigation.md","lastUpdated":1790314763000}'),s={name:"project-navigation.md"};function r(d,e,c,l,p,u){return i(),a("div",null,[...e[0]||(e[0]=[n('<h1 id="project-outline" tabindex="-1">Project outline <a class="header-anchor" href="#project-outline" aria-label="Permalink to &quot;Project outline&quot;">​</a></h1><p>Use <strong>LaTeX — Project Outline Window</strong> for the persistent tree (described below), or the original <strong>Project Outline</strong> dialog described here. Implementation and verification details are in <a href="./dev/outline-window">the window notes</a>.</p><p>Choose <strong>Scripts → LaTeX — Project Outline</strong> in a saved TeX document. Enter words from a heading, equation, caption, label, or filename; leave the search empty for the full outline. Search words are combined, with ASCII case-insensitive matching. Use <code>kind:label</code>, <code>kind:equation</code>, <code>kind:figure</code>, <code>kind:table</code>, or <code>kind:section</code> to restrict the entry type. Press Return to search, use the arrow keys to choose a result, and Return to go there. Escape cancels. Assign a shortcut through BBEdit&#39;s Scripts palette if desired; installation preserves existing shortcuts.</p><p>The outline displays section hierarchy, source paths and hard line numbers, equation text and figure/table captions. Labels remain separate entries, even when their names are duplicated. Partial-project warnings identify missing, dynamic, or cyclic inputs. Narrow searches with more than 300 matches.</p><p>This command reads <strong>saved files</strong>, starting at the same resolved main file as the build command. Each invocation rebuilds the small index without compiling TeX. Before a jump it checks the target file fingerprint, refuses an unsaved target buffer, and checks the destination line against the editor&#39;s contents. If a source changed, save/reload it as appropriate and invoke the outline again. Nothing is saved automatically. Other dirty buffers are not included in the index.</p><h2 id="scope-and-data-contract" tabindex="-1">Scope and data contract <a class="header-anchor" href="#scope-and-data-contract" aria-label="Permalink to &quot;Scope and data contract&quot;">​</a></h2><p><code>bbtex outline FILE [QUERY]</code> emits version-1 JSON with <code>root</code>, <code>savedOnly</code>, <code>issues</code>, and <code>entries</code>. Each entry has <code>kind</code>, <code>title</code>, canonical <code>file</code>, one-based <code>line</code>, hierarchy <code>depth</code>, nearby <code>context</code>, and the saved file&#39;s <code>fingerprint</code>. The additive <code>files</code> and <code>bibliographies</code> fields expose indexed-file fingerprints and declared bibliography paths for the citation/reference pickers. This index is reusable by future reference pickers. <code>outline-picker</code> and <code>outline-jump</code> are native-dialog integration commands.</p><p>The scanner follows literal <code>\\input</code>, <code>\\include</code>, and <code>\\subfile</code> paths, including bare <code>\\input</code> filenames. Relative paths are tried from the root directory first, then the including file&#39;s directory. Missing extensions default to <code>.tex</code>. Canonical paths prevent cycles; a file included repeatedly is indexed once. Limits are 256 files, 64 include levels, and 4 MiB per source file.</p><p>Recognized entries: part/chapter/section/subsection/subsubsection/paragraph/ subparagraph, labels, <code>\\[...\\]</code>, equation/align/gather/multline/flalign/displaymath/ eqnarray, and figure/table environments, including starred forms. Common macro definitions, comments, inline verb/lstinline, and verbatim/Verbatim/BVerbatim/ lstlisting/minted/comment environments are skipped. Titles retain TeX markup; no macro expansion, conditional evaluation, numbering, or bibliography parsing is attempted. Exotic definitions and custom environments remain outside scope. <code>\\includeonly</code> and import/subimport are reported as unsupported, rather than claiming an exact compilation graph. This is a source outline, not compiled output.</p><h2 id="native-capability-audit-and-presentation-decision" tabindex="-1">Native capability audit and presentation decision <a class="header-anchor" href="#native-capability-audit-and-presentation-decision" aria-label="Permalink to &quot;Native capability audit and presentation decision&quot;">​</a></h2><p>BBEdit&#39;s installed manual documents Search → Find Symbol in Workspace as a server-backed modal search, Go → Go to Definition, and the current-file function menu. These remain the preferred native symbol tools. The installed TexLab integration test confirms section document/workspace symbols alongside existing label/citation completion and definition lookup.</p><p>The installed BBEdit scripting dictionary exposes a functions-list palette but no supported API for retrieving its internal language-server symbol data. Starting a second TexLab session solely to mirror that data would require a new server lifecycle and buffer synchronization. The explicit saved-project outline therefore uses a bounded OCaml scanner and standard native dialogs. It adds stable duplicate-label entries and explicit snapshot validation without replacing the editor&#39;s existing symbol navigation or adding a persistent custom window.</p><h2 id="installation-and-verification" tabindex="-1">Installation and verification <a class="header-anchor" href="#installation-and-verification" aria-label="Permalink to &quot;Installation and verification&quot;">​</a></h2><p>Development: <code>scripts/install-workflow-commands.sh</code>. Release packages include the menu command and <code>project-outline.applescript</code> resource. Background errors are recorded in <code>~/.local/state/bbtex/outline.log</code>.</p><p><code>dune runtest</code> covers hierarchy, duplicates, include/root cycles, comments, literal environments, search, and edited files. Run <code>uv run test/integration/check_project_outline.py</code> to check the JSON contract and compile native dialogs. Set <code>BBTEX_TEST_NATIVE=1</code> to verify each fixture row&#39;s native mapping/jump and rejection of dirty buffers using disposable files. That automated test substitutes the dialog selection; actual keyboard behavior was confirmed by the user in BBEdit on 2026-09-21: search and Return-to-navigate work.</p><h2 id="persistent-outline-window" tabindex="-1">Persistent outline window <a class="header-anchor" href="#persistent-outline-window" aria-label="Permalink to &quot;Persistent outline window&quot;">​</a></h2><p>Choose <strong>Scripts → LaTeX — Project Outline Window</strong> from a saved TeX document. It opens an expandable, searchable outline and updates after saved-source changes. The original <strong>Project Outline</strong> dialog remains available.</p><p><img src="'+o+'" alt="The Project Outline Window listing sections, equations and labels across included files"></p><p>Each window stays pinned to the project shown at its top. Running the command from another file in that project reuses its window; running it from a different project opens a separate outline. Switching editor documents does not retarget an outline. Closing an outline stops its worker. Reopening starts a fresh session.</p><p>Refresh preserves search, expansion and the selected entry when their identities still exist. Navigation refuses unsaved target buffers: save, then refresh or wait for the automatic update. No document is saved automatically. The command stores private session files and a diagnostic <code>session.log</code> in the system temporary directory. The page is removed on close; the lock and log remain for reuse.</p>',20)])])}const f=t(s,[["render",r]]);export{m as __pageData,f as default};
