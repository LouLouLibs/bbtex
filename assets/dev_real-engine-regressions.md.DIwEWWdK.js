import{_ as i,o as t,c as n,a2 as a}from"./chunks/framework.Cv-B11KK.js";const p=JSON.parse('{"title":"Real-engine regression checks","description":"","frontmatter":{},"headers":[],"relativePath":"dev/real-engine-regressions.md","filePath":"dev/real-engine-regressions.md","lastUpdated":1790314763000}'),r={name:"dev/real-engine-regressions.md"};function o(s,e,c,d,l,u){return t(),n("div",null,[...e[0]||(e[0]=[a('<h1 id="real-engine-regression-checks" tabindex="-1">Real-engine regression checks <a class="header-anchor" href="#real-engine-regression-checks" aria-label="Permalink to &quot;Real-engine regression checks&quot;">​</a></h1><p>Run <code>dune build</code>, then <code>uv run test/integration/check_real_engines.py</code> for pdfLaTeX. Use <code>--engine xelatex</code> or <code>--engine lualatex</code> to run the same corpus with those engines. The selected engine, latexmk, BibTeX, Biber, and Poppler (<code>pdfinfo</code>, <code>pdftotext</code>, <code>pdftoppm</code>) must be on PATH. Python runs through uv with explicit script metadata. Required tools must start successfully; the runner fails rather than silently skipping them.</p><p>The original, MIT-licensed fixtures live in <code>test/corpus</code>. Every run copies them into a fresh directory containing spaces and Unicode. It never mutates <code>examples/ui-check</code> or other user projects. Cases cover:</p><ul><li>A two-column article with shared external macros, accented text, BibTeX, references, root directives, and a separate output directory.</li><li>A multi-file book with nested include paths, contents, biblatex/Biber, and Unicode bibliography metadata.</li><li>A widescreen Beamer deck with fragile/verbatim content.</li><li>Native Unicode/fontspec text under XeLaTeX and LuaLaTeX, using TeX-distributed Latin Modern font files without depending on OS font registration.</li><li>A generated 40-file article, including outline/index coverage.</li><li>Cropped preview geometry, PNG output, warm-cache reuse, external macro invalidation, and preservation of the compiled full-document PDF.</li><li>Included-file diagnostics with exact line mapping and a successful rebuild.</li><li>Cancellation of an actual looping TeX process and a subsequent successful build.</li></ul><p>Checks compare required PDF text, page counts, geometry ranges, resolved references, SyncTeX input records, and source/output isolation. They avoid cross-platform pixel comparisons. PNG differences are used only within one run to establish that a changed macro caused a new render.</p><p>Results remain under <code>dist/real-engines/ENGINE-*</code>, including copied source, build outputs, command stdout/stderr, diagnostic scripts, and <code>report.json</code> with tool versions, the selected engine, case timings and failure details. <code>--artifacts DIR</code> changes the parent directory; each run creates a new child directory. The first passing local run on 2026-09-22 took 14.6 seconds, excluding dependency installation. The expanded Unicode-inclusive corpus took 16.2 seconds with XeLaTeX and 21.1 seconds with LuaLaTeX locally on the same date.</p><h2 id="ci-tier" tabindex="-1">CI tier <a class="header-anchor" href="#ci-tier" aria-label="Permalink to &quot;CI tier&quot;">​</a></h2><p>Locally, <code>scripts/ci.sh --engines</code> runs this corpus with the installed TeX Live for pdfLaTeX, XeLaTeX and LuaLaTeX, and <code>--tectonic</code> adds the Tectonic tier with a cached Biber 2.17, which the pinned bundle&#39;s biblatex requires; see <a href="./releases">CI and releases</a>. The GitHub workflow below is kept for manual runs only.</p><p><strong>Real TeX engines</strong> runs a three-job Ubuntu 24.04 matrix for pdfLaTeX, XeLaTeX, and LuaLaTeX. Every job exercises BibTeX/Biber and the full shared corpus; the two Unicode engines also run the fontspec fixture. Generated large-project and cancellation fixtures explicitly select the requested engine. Each job uploads its own <code>real-engines-ENGINE</code> artifact, and one failure does not cancel the other jobs. TeX packages and Latin Modern fonts are explicit APT dependencies. OCaml and dune come from opam with the same pinned compiler as the package workflow. The workflow pins uv to 0.11.2; APT package versions follow the Ubuntu repositories and runtime versions are recorded in each report.</p><p>The workflow runs only when dispatched by hand. It uploads evidence on success or failure for 14 days.</p><p>An additional Tectonic job runs <code>uv run test/integration/check_tectonic.py --bundle https://data1.fullyjustified.net/tlextras-2022.0r0.tar --biber /path/to/compatible/biber</code>. It downloads the official 0.17.0 Linux musl binary and verifies its release SHA-256. The dated bundle is selected explicitly; resources are downloaded on demand on the fresh CI runner, then each fixture is rebuilt with <code>--only-cached</code>. This checks Tectonic&#39;s cached resource mode, not a network firewall. The bundle server remains a dependency; the archive itself is not vendored or independently checksum-pinned here.</p><p>This tier covers the BibTeX article, biblatex/Biber book, Beamer and Unicode/fontspec, PDF text/geometry, article/book SyncTeX, source/output isolation, and preview macro updates. Tectonic intentionally renders previews afresh (no recorder manifest), and the test asserts cache misses. It does not require <code>.bbl</code> persistence: Tectonic removes intermediates by default; the bibliography is checked in the rendered PDF. The tier also builds/indexes a generated 40-file project and cancels a real Tectonic process after console evidence confirms TeX reached its infinite loop. It verifies cancellation status, released ownership and a successful recovery build. Readiness uses flushed console messages because Tectonic buffers virtual files until processing finishes. The book checks rendered bibliography text, resolved citations and actual external Biber invocation on both online and cached runs.</p><p>The pinned 2022 bundle contains biblatex 3.17 and is tested with Biber 2.17. CI downloads the <a href="https://sourceforge.net/projects/biblatex-biber/files/biblatex-biber/2.17/binaries/Linux/" target="_blank" rel="noreferrer">official Biber 2.17 Linux release</a> into runner temporary storage and checks SHA-256 <code>129d2e0332a57e985ffa253e5e9fbd28ef99af5a068d1b141145211969aa8999</code>. The runner&#39;s <code>--biber</code> option creates a private executable link and changes PATH only for its own subprocesses. Without it, the runner uses Biber from PATH and fails if that version cannot launch or compile with the selected bundle. This historical test dependency is separate from the supported MacTeX baseline of Biber ≥ 2.21; do not replace system Biber with this older test tool. The official macOS universal archive used locally has SHA-256 <code>182e1efa074d8a2a23a8893f2a22440d4e463cce55e4ed02076ac4c0ee0614b2</code>; its arm64 slice in temporary storage passed the full tier in 14.1 seconds. The existing local four-engine preview test remains available. Native BBEdit/Skim, focus/selection, save attachments, and editing acceptance remain local checks; Linux rendering success does not establish native macOS behavior.</p><h2 id="findings-from-the-first-corpus-run" tabindex="-1">Findings from the first corpus run <a class="header-anchor" href="#findings-from-the-first-corpus-run" aria-label="Permalink to &quot;Findings from the first corpus run&quot;">​</a></h2><p>Nested <code>\\include{chapters/...}</code> failed with an isolated output directory on latexmk 4.85: file-line diagnostics prevented its missing auxiliary-directory recovery. bbtex now recognizes the specific missing <code>.aux</code> parent error, creates only relative directories inside the configured output tree, and retries latexmk with a forced build. It rejects traversal and symlink parents, and limits recovery to 16 retries. Other compiler failures remain errors.</p><p>The local TeX Live 2024 universal Biber launcher also failed during its arm64 extraction before processing any bibliography. The local passing run used a temporary arm64 extraction of the same Biber 2.20 executable, without replacing the installed TeX tools. The Ubuntu job uses its native packaged Biber. This is a local tool-launch issue to include in the planned doctor/troubleshooting work.</p><p>Tectonic&#39;s local book probe exposed a second, distinct Biber problem: the current cached bundle supplies biblatex 3.17 / control-file version 3.8, while the working temporary Biber 2.20 expects version 3.11. Tectonic does launch Biber automatically, but that pair fails. Biber-book coverage needs a compatible pinned tool/bundle pair; the Tectonic job now uses the isolated compatible pair described above. No installed TeX tools were changed for this coverage work.</p>',17)])])}const f=i(r,[["render",o]]);export{p as __pageData,f as default};
