import{_ as t}from"./chunks/preview-window.DO-cQqMu.js";import{_ as a,o as i,c as n,a2 as s}from"./chunks/framework.Cv-B11KK.js";const g=JSON.parse('{"title":"Preview selected mathematics","description":"","frontmatter":{},"headers":[],"relativePath":"selection-preview.md","filePath":"selection-preview.md","lastUpdated":1790314763000}'),o={name:"selection-preview.md"};function r(c,e,d,l,h,p){return i(),n("div",null,[...e[0]||(e[0]=[s('<h1 id="preview-selected-mathematics" tabindex="-1">Preview selected mathematics <a class="header-anchor" href="#preview-selected-mathematics" aria-label="Permalink to &quot;Preview selected mathematics&quot;">​</a></h1><p>Select a formula in BBEdit and run <strong>Scripts → LaTeX — Preview Selection</strong>. The command updates a compact PNG preview window inside BBEdit. No shortcut is assigned automatically. Move/resize that window beside your source; subsequent previews reuse its placement. The source retains focus and its selection. If you switch documents during rendering, first-window creation is skipped. The helper does not activate BBEdit when another app is active.</p><p><img src="'+t+`" alt="The preview window showing a rendered display equation with its source file, line and status"></p><p>For right-click access, install the BBEdit-only Quick Action:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">_build/default/bin/main.exe</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> preview-service</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span></span></code></pre></div><p>Then select text and choose <strong>right-click → Services → LaTeX — Preview Selection</strong>. macOS may place it directly in the contextual menu depending on the available services. It also appears under <strong>BBEdit → Services</strong>. If hidden, enable it in System Settings → Keyboard → Keyboard Shortcuts → Services. The action does not replace selected text. It invokes the installed Scripts-menu command, which must remain installed, either as a development command or in the standard release package location. Remove <code>~/Library/Services/LaTeX — Preview Selection.workflow</code> to uninstall the service. This service is a separate install from the BBEdit package. See <a href="https://developer.apple.com/library/archive/documentation/LanguagesUtilities/Conceptual/MacAutomationScriptingGuide/MakeaSystem-WideService.html" target="_blank" rel="noreferrer">Apple&#39;s service documentation</a>. Raw math such as <code>x^2 + y^2 = z^2</code> is wrapped in display math; complete <code>$…$</code>, <code>\\(…\\)</code>, <code>\\[…\\]</code>, and math environments such as <code>equation</code> or <code>align*</code> are used as selected. Include both opening and closing delimiters.</p><p>The preview uses the resolved main document&#39;s saved preamble, engine, default profile, and extra options. Preview does not save documents. For now, save any modified open project inputs explicitly before previewing; unsaved snapshots are not implemented yet. Relative preamble inputs are resolved from the main document&#39;s directory. There is no temporary edit to the source.</p><p>Cropping uses AUCTeX&#39;s standalone <a href="https://www.gnu.org/software/auctex/manual/preview-latex/The-LaTeX-style-file.html" target="_blank" rel="noreferrer"><code>preview.sty</code> package</a>, with <code>active,tightpage</code>. This reuses the LaTeX component, not Emacs&#39;s inline-image UI. Display equations can retain the document&#39;s text width while the page height is cropped. MacTeX normally provides this package; Tectonic may download it. PNG conversion requires Poppler&#39;s <code>pdftoppm</code> (already installed on this machine).</p><p>Preview output is stored in a separate <code>preview-…</code> directory under bbtex&#39;s state directory, with one reusable directory per main file. The full document&#39;s PDF and compiler log are preserved. A failed manual preview opens its own compiler log. Automatic failures stay in the preview window; choose <strong>LaTeX — Open Preview Log</strong> to inspect the log for the current preview request. <strong>LaTeX — Cancel Build</strong> also cancels a preview for the same project, and a preview cannot overlap a full build or cleanup of that project.</p><p>With no custom options, pdfLaTeX/XeLaTeX/LuaLaTeX run a single pass. Custom options retain the latexmk route, and Tectonic uses its own pipeline. The most recent render per root is cached for the three direct TeX engines. Cache checks cover the selection, preamble, engine/options, environment, root directory entries, engine/renderer executables, and files recorded by TeX&#39;s <code>.fls</code> recorder. Tectonic and custom-option builds are not cached yet. Time-dependent or random macros may need the cache removed manually; caching is intended for stable math. Remove the root&#39;s <code>preview-…/cache.inputs</code> to force regeneration.</p><p>On the simple test fixture, rendering measured 0.22–0.50 s, PNG conversion 0.06–0.16 s, and cache validation 0.03–0.10 s. The complete native command measured 1.45 s with first window creation and 0.31 s with a cached result and existing window. The window polls the published image every 250 ms, adding up to that delay before a change appears. These are local test timings, not guarantees.</p><h2 id="preview-status" tabindex="-1">Preview status <a class="header-anchor" href="#preview-status" aria-label="Permalink to &quot;Preview status&quot;">​</a></h2><p>The window shows the source file, cursor line, and one of <strong>Rendering</strong>, <strong>Current</strong>, <strong>Out of date</strong>, <strong>Preview failed</strong>, or <strong>Project busy</strong>. While rendering or after a failure, the previous image from that source may remain visible, dimmed and explicitly marked out of date. Switching source files clears the previous image. Current means the requested preview completed from saved inputs; it does not include unsaved changes in other editor buffers.</p><p>New requests replace older requests across manual and automatic preview. The running preview detects supersession and cancels its own compiler process group; it never cancels an unrelated full build. The final request check and image/status publication share a lock, so obsolete completions cannot overwrite newer state. The browser also rejects out-of-order responses and delayed image loads. Existing open preview pages migrate automatically when the next request arrives.</p><h2 id="limits" tabindex="-1">Limits <a class="header-anchor" href="#limits" aria-label="Permalink to &quot;Limits&quot;">​</a></h2><ul><li>The root must contain a literal <code>\\begin{document}</code>. The extractor skips normal TeX comments but does not expand macros or interpret conditional TeX code.</li><li>Only the preamble and selection are compiled. Definitions made earlier in the document body, surrounding environments, document counters, references, and bibliography state are not reconstructed. Use a full build when they matter.</li><li>Select complete math, not a partial environment or a whole document. Raw text is treated as math. This is not a general figure/table extraction command.</li><li>Package hooks and custom build options still execute as in normal compilation. Documents with unusual output routines may not support the preview package.</li></ul><h2 id="verification" tabindex="-1">Verification <a class="header-anchor" href="#verification" aria-label="Permalink to &quot;Verification&quot;">​</a></h2><p><code>test/integration/check_preview.py</code> compiles raw math and <code>align*</code> with pdfLaTeX, XeLaTeX, LuaLaTeX, and Tectonic, including a relative preamble input. It checks PDF dimensions, unchanged project files, empty selections, and failure logs. <code>test/integration/check_live_preview.py</code> exercises selection capture, PNG publication, preview-window reuse, and source-focus preservation in BBEdit. <code>test/integration/check_live_selection.py</code> exercises the live-selection watcher end to end — selection to <strong>Current</strong>, cached reuse, an invalid selection, and watcher shutdown on window close — and measures the latencies in <a href="#live-selection">Live selection</a>. The actual HTML image refresh still benefits from user visual verification. Run all three with <code>uv run</code>; their dependencies are declared inline.</p><h2 id="refresh-on-save" tabindex="-1">Refresh on save <a class="header-anchor" href="#refresh-on-save" aria-label="Permalink to &quot;Refresh on save&quot;">​</a></h2><p>Run <strong>LaTeX — Toggle Preview on Save</strong> from BBEdit’s Scripts menu for a saved TeX file. With the cursor inside a displayed equation, press <strong>⌘S</strong> to refresh the PNG preview. Run the command again in that file to disable it. Enabling it in another file switches the tracked file. It is off by default; closing the preview window does not disable it.</p><p>This uses BBEdit’s <code>documentDidSave</code> attachment, not continuous typing capture. The initial implementation recognizes <code>\\[...\\]</code>, equation, align, gather, multline, flalign (including starred forms), and displaymath environments. Inline math and <code>$$...$$</code> are not recognized. Place the cursor inside a complete block; saves elsewhere mark the previous preview out of date. Included files use the existing main-file resolver and the main file’s <strong>saved</strong> preamble.</p><p>After an equation save, saving its resolved main file or a known input (such as an included macro file) refreshes that equation using its retained source and line. Foreground and background document saves work; unrelated saves are ignored. Dependency refresh updates an existing preview without opening a new window or moving focus. If the tracked source has changed on disk, save it with the cursor inside the intended equation to establish a fresh location. A manual selection preview replaces the automatic anchor; save an equation to resume tracking.</p><p>Known inputs come from the TeX recorder and survive cache invalidation and failed renders. pdfLaTeX, XeLaTeX, and LuaLaTeX supply them, including the latexmk path for custom options when recording remains enabled. For engines without recorder output, including Tectonic, only resolved-main-file refresh is guaranteed. Unsaved buffers and edits made outside BBEdit do not trigger the save attachment.</p><p>Rapid saves are coalesced and superseded renders are cancelled. Normal compilation suppresses the hook, interrupts the matching automatic preview, and waits for its compiler cleanup before starting the full build. The old preview is marked out of date; save an equation afterward to refresh it. Disabling or switching tracking also invalidates the old automatic request. Cancellation includes a short grace period for compiler children to stop.</p><p>Install the menu command with <code>scripts/install-workflow-commands.sh</code> and the attachment with <code>scripts/install-preview-save-hook.sh --apply</code>. The installer refuses to replace unrelated save attachments; those require manual integration. Background diagnostics are in <code>~/.local/state/bbtex/preview-on-save.log</code>.</p><h2 id="release-installation" tabindex="-1">Release installation <a class="header-anchor" href="#release-installation" aria-label="Permalink to &quot;Release installation&quot;">​</a></h2><p>Install <code>bbtex.bbpackage</code> in <code>~/Library/Application Support/BBEdit/Packages/</code>. For the optional save hook and contextual service, run the bundled installers:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$HOME</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">/Library/Application Support/BBEdit/Packages/bbtex.bbpackage/Contents/Resources/install-preview-save-hook.sh&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --apply</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$HOME</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">/Library/Application Support/BBEdit/Packages/bbtex.bbpackage/Contents/Resources/bbtex&quot;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> preview-service</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span></span></code></pre></div><p>The attachment is not activated merely by copying a package into BBEdit. These installers add no duplicate Scripts-menu commands, and neither enables save tracking. Existing development menu links take precedence; use either the development setup (with its support package) or the full release package to avoid duplicate menus. Do not install the full package alongside development scripts. Package-only installs must retain the standard name and location above. The serialization helper uses macOS&#39;s <code>/usr/bin/perl</code>. The optional installers are a bash script and a <code>bbtex</code> subcommand; they need no Python or uv.</p><p><code>test/integration/check_save_preview_worker.py</code> covers rapid saves, source switching, disabling tracking, saves during publication, build suppression, dependency routing, invalidated source locations, and failure status without native automation. <code>check_preview_cancellation.py</code> checks process-group cleanup and isolation from full builds. <code>check_snippet_page.mjs</code> checks the browser&#39;s ordering and safe text rendering with controlled image loads. <code>check_save_preview.py</code> checks real BBEdit equation saves, foreground/background macro saves, focus preservation, and a rendering error with disposable documents. It retains a temporary recovery backup and restores the prior tracking flag/image with a fresh publication revision.</p><h2 id="live-selection" tabindex="-1">Live selection <a class="header-anchor" href="#live-selection" aria-label="Permalink to &quot;Live selection&quot;">​</a></h2><p>Run <strong>LaTeX — Toggle Live Selection Preview</strong> from BBEdit&#39;s Scripts menu in a saved TeX file to make the preview window follow the selection. Run the command again to turn it off. Enabling live selection turns off Preview on Save, and enabling Preview on Save turns off live selection.</p><p>A poller watches BBEdit&#39;s front window and selection: every 0.15 s while BBEdit is frontmost, every 1 s otherwise. A selection renders once it has been unchanged for 0.35 s, so drags and shift-arrow extension render only their final state. Moving the cursor without selecting, or selecting only whitespace, leaves the last preview unchanged. Reselecting a range after the selection moved renders it again, so an edited fragment refreshes; switching to another application and back does not. Unchanged fragments come from the render cache.</p><p>The selected fragment comes from the live, unsaved buffer; the preamble and dependencies still come from saved files, as in manual preview. Unlike manual preview, live mode does not refuse dirty buffers — the current document is almost always dirty while editing. <strong>Current</strong> means the preview matches the selection and was rendered with the saved preamble.</p><p>Delimited math (<code>$…$</code>, <code>\\(…\\)</code>, <code>\\[…\\]</code>, and math environments such as <code>equation</code> or <code>align*</code>) is used as selected. Raw text renders as math when the selection starts inside math in the buffer — as <code>align*</code> if it contains <code>&amp;</code> or <code>\\\\</code>, otherwise as <code>\\[...\\]</code> — and as a prose paragraph at text width otherwise. Unbalanced braces or environments, an unclosed math delimiter, and a selection containing <code>\\documentclass</code>, <code>\\begin{document}</code>, or <code>\\end{document}</code> are rejected with a message; the previous image stays, dimmed. Selections over 20 KB are rejected.</p><p>Live renders queue behind save previews and full builds on the same lock, so back-to-back selections never race for the project build lock and a build waits for a running render instead of being refused. While a full build is active, live rendering pauses — the window shows <strong>Project busy</strong> — and the settled selection renders once the build ends. The build is never cancelled for live preview. A render cancelled by a save or a build shows <strong>Preview cancelled</strong>; select again to retry.</p><p>Manual <strong>Preview Selection</strong> and a live render still share the project&#39;s build lock without queueing: if both run at once, the one that loses reports <strong>Project busy</strong> (manual preview as an alert).</p><p>The watcher stops when live mode is toggled off, when the preview window has been closed for 2 s, when no preview window appears within 10 s of starting, or when BBEdit quits. If the toggle fails to start the watcher, it notifies and stops the watcher rather than leaving it running with no window.</p><p>Diagnostics are in <code>$BBTEX_STATE_DIR/live-selection.log</code> (<code>~/.local/state/bbtex/</code> by default). Manual <strong>Preview Selection</strong> is unchanged.</p><p>Measured locally on 2026-09-24 (Apple Silicon, BBEdit 15.5.5, pdfLaTeX) with <code>test/integration/check_live_selection.py</code>: 1.23 s from selection to <strong>Current</strong> on a cold render, 0.76 s when the render is cached, both including the debounce and poll. These are local timings, not guarantees.</p>`,40)])])}const v=a(o,[["render",r]]);export{g as __pageData,v as default};
